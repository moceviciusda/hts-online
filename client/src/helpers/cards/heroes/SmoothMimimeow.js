import Card from "../Card";

export default class SmoothMimimeow extends Card {
    constructor(scene) {
        super(scene)
        this.name = 'smoothMimimeow'
        this.cardSprite = 'smoothMimimeow'
        this.type = 'hero'
        this.class = 'thief'
    }
}