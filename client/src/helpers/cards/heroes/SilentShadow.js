import Card from "../Card";

export default class SilentShadow extends Card {
    constructor(scene) {
        super(scene)
        this.name = 'silentShadow'
        this.cardSprite = 'silentShadow'
        this.type = 'hero'
    }
}