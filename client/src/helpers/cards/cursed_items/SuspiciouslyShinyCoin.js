import Card from "../Card";

export default class SuspiciouslyShinyCoin extends Card {
    constructor(scene) {
        super(scene)
        this.name = 'suspiciouslyShinyCoin'
        this.cardSprite = 'suspiciouslyShinyCoin'
        this.type = 'item'
    }
}