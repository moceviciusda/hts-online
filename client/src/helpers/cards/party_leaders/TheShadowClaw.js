import LeaderCard from "./LeaderCard"

export default class TheShadowClaw extends LeaderCard {
    constructor(scene) {
        super(scene)
        this.name = 'theShadowClaw'
        this.cardSprite = 'theShadowClaw'
    }
}