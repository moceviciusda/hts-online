import LeaderCard from "./LeaderCard"

export default class TheFistOfReason extends LeaderCard {
    constructor(scene) {
        super(scene)
        this.name = 'theFistOfReason'
        this.cardSprite = 'theFistOfReason'
    }
}