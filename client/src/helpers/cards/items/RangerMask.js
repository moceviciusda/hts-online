import Card from "../Card";

export default class RangerMask extends Card {
    constructor(scene) {
        super(scene)
        this.name = 'rangerMask'
        this.cardSprite = 'rangerMask'
        this.type = 'item'
    }
}