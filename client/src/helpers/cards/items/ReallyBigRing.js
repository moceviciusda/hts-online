import Card from "../Card";

export default class ReallyBigRing extends Card {
    constructor(scene) {
        super(scene)
        this.name = 'reallyBigRing'
        this.cardSprite = 'reallyBigRing'
        this.type = 'item'
    }
}